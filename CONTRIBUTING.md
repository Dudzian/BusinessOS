# Contributing

Use feature branches and pull requests. Do not merge to `main` unless Windows CI is green, checks are current, at least one review is approved, and direct pushes to `main` are blocked by repository settings.
