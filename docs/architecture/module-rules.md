# Module rules

Each module exposes one registration method, for example `AddCompaniesModule`, `AddBusinessProjectsModule` and `AddBudgetingModule`. Cross-module communication must use explicit contracts or internal events.
