# Data isolation

Full company data isolation is deferred until the persistence/application workflow block. Domain models already carry company identifiers where required, but no repository-level isolation is implemented in Block 1.
